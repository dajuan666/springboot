<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	
<mapper namespace="com.liu.mapper.IContentMapper">
	<select id="queryContent" resultType="content">
		SELECT
			id,
			title,
			content,
			create_time,
			collections,
			keywords,
			img,
			status,
			description
		FROM
			mk_content ORDER BY create_time desc
	</select>
</mapper>